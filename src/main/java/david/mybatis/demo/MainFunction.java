package david.mybatis.demo;

import java.util.Arrays;
import java.util.Calendar;

import david.mybatis.model.BasicQueryArgs;
import david.mybatis.model.Channel;
import david.mybatis.model.SortDirectionEnum;
import david.mybatis.model.Visitor;
import david.mybatis.model.Website;

public class MainFunction {

	public static void main(String[] args) {

//		 DemoRun.addVisitors();
		// DemoRun.addVisitor(new Visitor("新增测试DB", "mysqldb@gmail.com"));
//		 DemoRun.queryVisitor(11);
		// DemoRun.updateVisitor(11);
		// DemoRun.deleteVisitor(11);
		// DemoRun.queryVisitorList();
		// System.out.println("----------分割线----------");
		// DemoRun.queryVisitorListWithPagenate(1, 5, "id",
		// SortDirectionEnum.DESC.toString());
		// System.out.println("--------------具有Rownum的显示方式---------------");
		// DemoRun.queryVisitorListWithRnPagenate(1, 5, "id",
		// SortDirectionEnum.DESC.toString());
		// DemoRun.addWebsite(new Website("果壳网", 4));
		// DemoRun.queryWebsite(2);
		// DemoRun.deleteWebsite(3);
		// DemoRun.updateWebsite(2);
		// DemoRun.addWebsites();
		// DemoRun.queryWebsiteListByVisitorId(new BasicQueryArgs(2));
		// DemoRun.queryWebsiteList();

		// DemoRun.addChannel(new Channel("社交频道", 6));
		// DemoRun.addChannels();
		// DemoRun.queryChannel(1);
		// DemoRun.deleteChannel(2);
		// DemoRun.updateChannel(1);
		// DemoRun.queryChannelList();
		// DemoRun.queryChannelListBySiteId(6);
		// DemoRun.queryChannelListByVisitorId(2);
		// DemoRun.queryChannelListByVisitorId(Arrays.asList(1, 10, 16));

		// DemoRun.retrieveListCount();
		// DemoRun.testBasicQuery(1);
		// DemoRun.testBasicQueryByInterfaceWay(2);

		// Calendar calendar = Calendar.getInstance();
		// calendar.set(2013, 11, 18);
		//
		// DemoRun.getListChooseWhenDemo(0, null, calendar.getTime());
		// System.out.println("---------------分割线-------------");
		// DemoRun.getListWhereCondition(25, null, calendar.getTime());
		//
		// DemoRun.getListForeachDemo(Arrays.asList(4, 5, 6));
	}
}
